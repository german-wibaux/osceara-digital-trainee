import { BoolActivePipe } from './bool-active.pipe';

describe('BoolActivePipe', () => {
  it('create an instance', () => {
    const pipe = new BoolActivePipe();
    expect(pipe).toBeTruthy();
  });
});
